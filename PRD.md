# PRD — Lore Guard
## 세계관 정합성·용어 일관성 QA 에이전트 (내러티브 CI)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 (2026-07-31) |
| 작성자 | 예건 |
| 상태 | 초안 — 확인 대기 |
| 대상 IP | 크로노 오디세이 (Chrono Odyssey) — 크로노스튜디오 개발, 카카오게임즈 퍼블리싱 |
| 포지셔닝 | 포트폴리오 프로젝트 #2 — "회사가 지켜야 할 자산(세계관)을 지키는 도구" |
| 관련 문서 | 지침서(LOREGUARD_DIRECTIVE.md), User Flow(PRD 확정 후 작성) |

---

## 1. 배경과 문제 정의

### 1.1 문제

방대한 세계관을 다루는 소규모 내러티브 팀에서 실제로 발생하는 사고는 다음 네 가지로 수렴한다.

1. **표기 불일치** — 같은 고유명사가 문서마다 다르게 표기된다.
2. **연표 충돌** — 사건의 시점·순서가 텍스트 간에 어긋난다.
3. **번역 비일관** — 한↔영 고유명사 대응이 파일마다 다르다.
4. **설정 모순** — 새로 쓴 텍스트가 기존 설정과 충돌한다.

현재 이 검증을 수행하는 유일한 도구는 **리드 작가의 기억**이다. 인력 규모가 작을수록 이 단일 장애점(SPOF)의 리스크는 커진다.

### 1.2 왜 크로노 오디세이인가 — 조사로 확인된 사실

사전 조사(2026-07-31)에서 다음이 확인되었다.

- **데이터 성립**: 공식 Fandom 위키(크로노스튜디오가 2025-05-16 직접 공개, 미공개 자료 포함), Fextralife 위키, chronoodysseywiki.com, 스팀 페이지 한/영 병행 텍스트, 개발자 노트 1~5차(최신 2026-05-07), 세계관 소개 개발자 영상 — 모두 공개 데이터로 접근 가능.
- **세계관 복잡도 충분**: 12행성 × 12센티넬 × 시간여행(1년 회귀) 구조. 연표 충돌과 명칭 불일치가 구조적으로 발생하기 쉬운 유형.
- **실제 불일치 사례가 이미 존재** (데모 설득력 보장):
  - 인물명 표기 갈림: 뱅가드 지휘관이 소스에 따라 `Etheldrena`(Fextralife) / `Etheldreda`(요약 소스)로 병기됨.
  - 소멸 용어: 초기 보도(2020~2021)의 조직명 `이드리긴`은 현행 공식 자료에서 사라짐. "12명의 신에게 대항" 서사도 현행 "보이드 대항" 서사와 다름 — 설정 개편 이력의 실증 사례.
  - 총괄 개발자 표기: 초기 보도 `배봉건` vs 현행 `윤상태` — 시점별 사실 관리 필요성의 사례.
- **게임이 살아있는 대상**: 2026년 출시 목표로 개발자 노트가 계속 갱신 중 → "패치노트 대응" 기능이 실전에서 작동함을 시연할 기회가 계속 생긴다.

### 1.3 프로젝트가 전달하는 메시지

> "당신들이 가장 아끼는 자산(세계관)을, 가장 큰 제약(인력 규모)에도 불구하고 지킬 수 있게 하는 도구."

프로젝트 1(유저가 본 문제)과 결합해 "이 게임을 양쪽에서 이해한 지원자"로 읽히게 한다. 코드에 CI가 있듯 내러티브에 CI를 다는 개념은 LLM 이전에는 성립 불가능했던 영역이다.

---

## 2. 목표와 성공 지표

### 2.1 목표

| # | 목표 | 검증 방법 |
|---|---|---|
| G1 | 공개 데이터만으로 크로노 오디세이 세계관 지식베이스를 관계망으로 구조화 | 엔티티 40+ / 팩트 120+ / 관계 60+ 등록 |
| G2 | 실존 불일치를 최소 3건 이상 자동 탐지 | §1.2의 실증 사례가 검사 리포트에 실제로 잡힘 |
| G3 | 로컬라이제이션 팀이 바로 쓸 수 있는 형태의 한영 용어집 자동 편찬 | CSV/JSON 내보내기, 항목당 출처 링크 포함 |
| G4 | 신규 텍스트 검증 게이트 라이브 데모 성립 | 가상 퀘스트 대사 입력 → 30초 내 PASS/WARN/FAIL 판정 + 근거 팩트 인용 |
| G5 | 지식베이스가 "살아있는" 시스템임을 증명 | 신규 개발자 노트 유입 → 변경 감지 리포트 자동 생성 |
| G6 | 복선(떡밥) 원장 운영 | 미해소 복선 10+ 등록, 신규 텍스트 유입 시 재부상 탐지 |

### 2.2 비목표 (Out of Scope)

- 게임 클라이언트 데이터마이닝, 비공개 자료 수집 — **금지**. 회사가 스스로 공개한 데이터만 사용.
- 원문 전문(全文)의 저장·재배포 — 저작권 원칙 위반 (§6).
- 실시간 크롤링 봇 상시 운영 — 수집은 수동 트리거 파이프라인으로 충분.
- 다국어 3개 이상 확장 — v1은 한↔영만.
- 사용자 계정/권한 시스템 — 포트폴리오 데모에 불필요.

---

## 3. 타깃 사용자 (페르소나)

| 페르소나 | 상황 | Lore Guard가 주는 것 |
|---|---|---|
| **리드 작가** | 자기 머릿속이 유일한 검증 도구. 신규 텍스트 검수에 병목 | 검증 게이트에 붙여넣기 → 즉시 충돌 판정. 기억 부담의 외부화 |
| **로컬라이제이션 담당** | 고유명사 번역이 파일마다 달라 매번 수작업 대조 | 출처 링크가 달린 한영 용어집을 CSV로 즉시 수령 |
| **신입 내러티브 작가** | 방대한 설정을 학습할 진입로가 없음 | 관계망 탐색 UI로 세계관 온보딩 |
| **채용 심사자** (실제 1차 사용자) | 지원자의 리서치 깊이와 실행력을 3분 안에 판단해야 함 | 라이브 데모: 실존 불일치가 잡히는 순간 + 검증 게이트 시연 |

---

## 4. 핵심 기능

### F1. 세계관 지식베이스 (Knowledge Base)

공개 소스에서 추출한 사실을 **엔티티 — 팩트 — 관계 — 출처**의 4층 구조로 관리한다.

**엔티티 타입**: `character`(인물) / `location`(지역) / `faction`(세력) / `artifact`(유물) / `event`(사건) / `planet`(행성) / `concept`(개념) / `creature`(존재)

**시드 엔티티 (조사에서 확보, 최소 세트)**:
- 행성: 세테라(Setera), 로디니아(Rodinia), 마젤란(Magellan) + "12행성" 개념
- 인물: 크로노스(Chronos), 벨리아(Velia), 베텔기우스(Betelgeuse), 이오비스(Iobis), 알렉시오스(Alexios), 발두르(Baldur), 에셀드레나(Etheldrena/Etheldreda — 충돌 상태로 등록), 카야(Kaya), 테오파네스 5세(Theophanes V), 사비오(Sabio), 로멕(Romek), 가이아나(Gaiana), 주인공(세테라의 센티넬)
- 세력: 프론티어(Frontier), 뱅가드(Vanguard), 정착민(Settlers), 데스리스(Deathless), 브로큰(Broken), 월드 무버(World Movers), 크로노스 교단(Chronos Order)
- 유물/개념: 크로노텍터(Chronotector), 시간의 유적(Ruins of Time), 시간의 균열(Temporal Rift), 통찰의 눈(Eye of Insight), 아르케(Arche), 보이드(Void), 센티넬(Sentinel), 크로노 게이트(Chrono Gate), 보이드 넥서스(Void Nexus), 에덴(Eden)
- 지역: 새벽 비탈(Dawn Slope), 소로마 요새(Soroma Stronghold)

**팩트 스키마** (모든 팩트는 이 구조를 따른다):

```json
{
  "id": "FACT-0017",
  "entity_ids": ["ENT-BETELGEUSE", "ENT-VOID"],
  "claim_type": "timeline",
  "statement_ko": "베텔기우스가 시간의 균열에서 보이드를 깨워 12행성에 풀어놓았다.",
  "statement_en": "Betelgeuse awakened the Void from the Temporal Rift and unleashed it upon the twelve worlds.",
  "sources": [
    { "url": "https://chrono-odyssey.fandom.com/wiki/Chrono_Odyssey",
      "source_tier": "official-wiki", "captured_at": "2026-07-31" }
  ],
  "status": "active",
  "superseded_by": null,
  "confidence": "confirmed",
  "first_seen_kb_version": "2026.07",
  "notes": ""
}
```

- `claim_type`: `timeline` / `relation` / `attribute` / `naming` / `tone-canon`
- `status`: `active`(현행) / `superseded`(변경됨→후속 팩트 링크) / `deprecated`(공식 폐기) / `conflicted`(소스 간 충돌)
- `source_tier`: `official-site` > `steam-official` > `dev-note` > `official-wiki` > `community-wiki` > `press`. 충돌 시 상위 티어 우선을 기본 규칙으로 하되, 최종 판정은 리포트에 근거와 함께 표기.
- `confidence`: `confirmed`(공식 소스 2개 이상) / `probable`(단일 공식 소스) / `speculative`(커뮤니티 단독)

**저장 방식**: 파일 기반 JSON + Git. DB를 두지 않는 것이 설계 의도다 — **지식베이스의 모든 변경이 git diff로 남아**, "패치노트 대응"(F6)이 커밋 히스토리 그 자체가 된다.

### F2. 정합성 검사 엔진

5종 검사를 실행하고 위반 항목을 심각도(`critical` / `warning` / `info`)와 함께 리포트로 출력한다. C1·C2·C4는 의미 이해가 필요하므로 LLM(Claude API) 판정, C3·C5는 규칙 기반 + LLM 보조의 하이브리드.

| ID | 검사 | 방식 | 실증 예시 (조사 확보) |
|---|---|---|---|
| C1 | 연표 충돌 | 팩트 쌍의 시간 관계를 LLM이 대조 | "1년 전 과거 회귀" 전후 사건 순서 검증 |
| C2 | 설정 모순 | 동일 엔티티의 팩트 집합에서 양립 불가 진술 탐지 | 초기 "12신에 대항" vs 현행 "보이드에 대항" |
| C3 | 표기 일관성 | 이름 변형(오탈자·로마자 변형) 편집거리 + LLM 동일인 판정 | `Etheldrena` vs `Etheldreda` |
| C4 | 톤 위반 | 중세 다크 판타지 톤 캐논 대비 문장 린트 | 현대어·밈·과도한 경어 등 탐지 (검증 게이트에서 주로 사용) |
| C5 | 미해소 복선 | "언급됐으나 회수되지 않은 설정" 추출 → F5 원장으로 전달 | "고대 유적들이 비밀을 드러내고 있다" 류 문장 |

**검사 리포트 스키마**: 위반 ID, 검사 타입, 심각도, 관련 팩트 ID들, 판정 근거(LLM 설명), 권고 조치(예: "용어집 기준 표기를 Etheldrena로 통일 제안"), 리포트 생성 시각.

### F3. 용어집(Glossary) 자동 생성

- 지식베이스의 `naming` 팩트에서 자동 편찬: **한글 표기 / 영문 표기 / 로마자 변형 / 정의(1문장) / 엔티티 타입 / 출처 링크 / 신뢰도 / 상태**.
- 충돌 표기는 숨기지 않고 `⚠ 표기 충돌` 배지와 함께 병기 — 용어집의 정직성이 도구의 신뢰성이다.
- 내보내기: CSV(로컬라이제이션 툴 호환), JSON. 웹 UI에서 검색·필터(타입/상태/신뢰도).

### F4. 신규 텍스트 검증 게이트 (내러티브 CI) — 라이브 데모의 핵심

- 입력: 신규 텍스트(가상 퀘스트 대사, NPC 대화 등)를 텍스트박스에 붙여넣기.
- 처리: 텍스트에서 엔티티·주장 추출 → 지식베이스 대조 → C1~C4 검사 실행.
- 출력: **PASS**(충돌 없음) / **WARN**(주의 — 근거 약함·톤 이슈) / **FAIL**(명백한 설정 충돌) + 각 판정에 근거 팩트 카드 인용 + 수정 제안.
- 성능 목표: 판정 완료까지 30초 이내(스트리밍 표시로 체감 개선).
- **데모 시나리오 3종을 사전 제작**하여 원클릭 실행 버튼 제공:
  1. PASS 케이스 — 기존 설정과 부합하는 무난한 대사
  2. FAIL 케이스 — "벨리아가 주인공을 10년 전으로 보냈다" (정답: 1년) 류의 명백한 연표 충돌
  3. WARN 케이스 — 다크 판타지 톤을 깨는 현대적 어휘가 섞인 대사

### F5. 복선 원장 (Foreshadowing Ledger) — 차별화 기능

정합성 검사의 부산물인 "미해소 설정"을 버리지 않고 자산으로 축적한다.

- **생명주기**: `unresolved`(미해소) → `resurfaced`(재언급 — 신규 텍스트에서 다시 등장) → `resolved`(회수) / `abandoned`(폐기 추정)
- 각 복선 항목: 제목, 요약, 최초 발견 소스·시점, 연결된 팩트 ID, 상태 이력(타임라인), 재부상 트리거가 된 텍스트.
- **핵심 동작**: F6 파이프라인으로 신규 텍스트가 유입될 때마다 기존 미해소 복선과의 의미적 연결을 LLM이 탐지 → 대시보드 "이번 업데이트로 재부상한 복선" 섹션에 강조 표시.
- 시드 복선 후보(조사 확보): 세테라의 고대 유적이 드러내는 비밀 / 보이드에 삼켜진 11개 행성의 행방 / 벨리아가 주인공을 과거로 보낸 진짜 이유 / 크로노스 교단과 가이아나의 예언 / 월드 무버 3개 분파의 경쟁 구도.

### F6. 변경 감지 파이프라인 (패치노트·개발자 노트 대응)

지식베이스를 스냅샷이 아닌 **버전드 시스템**으로 운영하는 장치.

- 입력: 신규 소스 URL(개발자 노트, 패치노트, 공지) 등록 → 수집 스크립트 실행.
- 처리: 신규 텍스트에서 팩트 추출 → 기존 지식베이스와 3분류 대조:
  - **NEW**: 기존에 없던 사실 → `active`로 추가
  - **CHANGED**: 기존 팩트와 충돌 → 기존 팩트 `superseded` 처리 + 신규 팩트 링크 + 변경 리포트 기재
  - **CONFIRMED**: 기존 팩트 재확인 → `confidence` 상향
- 출력: **변경 감지 리포트**(KB 버전 `YYYY.MM.patch` 태그) + 복선 재부상 탐지(F5 연동) + git 커밋.
- 이 기능이 도구를 "한 번 만든 분석"이 아니라 "게임과 함께 자라는 시스템"으로 만든다. 출시(2026년 목표) 이후 실제 패치노트가 나올 때마다 갱신 시연 가능.

### F7. 대시보드 (탐색 + 리포트)

| 화면 | 내용 |
|---|---|
| ① 개요(Overview) | KB 버전, 엔티티/팩트/충돌/미해소 복선 카운트, 최근 변경 감지 요약, "재부상한 복선" 하이라이트 |
| ② 지식베이스 탐색 | 관계망 그래프(엔티티 노드 + 관계 엣지, 클릭 시 팩트 카드 패널) + 리스트/검색 뷰 병행 |
| ③ 검사 리포트 | C1~C5 위반 목록, 심각도 필터, 근거 팩트 링크 |
| ④ 용어집 | 검색·필터 테이블, 충돌 배지, CSV/JSON 내보내기 |
| ⑤ 검증 게이트 | 텍스트 입력 → 스트리밍 판정 → 근거 카드. 데모 시나리오 3종 버튼 |
| ⑥ 복선 원장 | 생명주기별 칸반/타임라인 뷰 |

---

## 5. 기술 사양

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + React | LLM 호출용 서버 라우트 필요, Vercel 배포 파이프라인 기존 워크플로우와 동일 |
| 스타일 | Tailwind CSS v4 | 기존 프로젝트와 통일 |
| 관계망 시각화 | 경량 그래프 라이브러리(d3-force 기반) 또는 자체 SVG 렌더 — 지침서에서 결정 | 노드 100개 미만 규모라 중량 라이브러리 불필요 |
| LLM | Claude API (회사 제공 API 키, 서버 라우트에서만 호출) | 검사 C1~C5, 팩트 추출, 검증 게이트 판정 |
| 데이터 | 파일 기반 JSON (`/data`) + Git 버전 관리 | KB 변경 이력 = git 히스토리 (F6의 설계 근간) |
| 수집 | 로컬 Node 스크립트(수동 트리거) — 소스 URL 목록 → 팩트 추출 → JSON 갱신 | 상시 크롤러 배제(§2.2), 저작권 원칙 준수(§6) |
| 배포 | Vercel | 기존 워크플로우 동일 |
| 검증 | Playwright 스크린샷 검증 | 기존 워크플로우 동일 |

**API 키 취급**: 서버 환경변수로만 관리. 클라이언트 노출 금지. 검증 게이트는 서버 라우트 경유.

---

## 6. 저작권·데이터 윤리 설계 (필수 요건)

프로젝트 1에서 검증한 원칙을 그대로 적용하며, 코드 수준에서 강제한다.

1. **원문 전문 비저장**: 수집 파이프라인은 원문에서 *사실(팩트)만 추출*하여 자체 문장으로 재기술해 저장한다. 원문 텍스트 파일을 리포지토리에 두지 않는다.
2. **인용 최소화**: 팩트 카드에 원문 인용을 표시해야 할 경우에도 15단어 미만·항목당 1회로 제한. 기본은 재기술.
3. **출처 링크 참조**: 모든 팩트·용어집 항목은 원문으로의 링크를 보유 — 저장이 아니라 참조.
4. **비공식 고지**: 앱 전역 푸터 + README에 "본 프로젝트는 크로노스튜디오·카카오게임즈와 무관한 비공식 팬 포트폴리오이며, 크로노 오디세이의 모든 권리는 해당 권리자에게 있습니다" 명시.
5. **공개 데이터 한정**: 데이터마이닝·유출 자료·비공개 빌드 텍스트 사용 금지. 소스 등록 시 티어 분류로 필터링.
6. ~~**이미지 미사용**: 게임 아트·스크린샷을 UI에 사용하지 않는다. 비주얼 아이덴티티는 자체 디자인으로 해결(§7).~~
   > **[개정 2026-08-06 — 위 조항은 아래 6′로 대체(superseded)됨. 이력 보존을 위해 원문은 삭제하지 않는다.]**
6′. **이미지 제한 사용**: 비주얼 아이덴티티는 여전히 자체 디자인으로 해결한다(§7). 다만 **공식 배포 채널(Steam 상점 페이지)의 프로모션 스크린샷**에 한해, 연대기 화면에서 출처·저작권 표기와 함께 제한적으로 사용할 수 있다(`/public/chronicle/`). 데이터마이닝·유출·비공개 빌드 이미지는 §6.5와 동일하게 금지.
   - **개정 사유**: 연대기가 첫 화면의 서사 축으로 확정되면서 장별 시각 앵커가 필요해졌고, 자체 일러스트 제작은 이 프로젝트의 범위 밖이다. 권리자가 공개 배포 채널에서 스스로 홍보 목적으로 배포하는 이미지에 한해, 출처·저작권·비공식 고지를 함께 표기하는 조건이라면 §6의 나머지 원칙(원문 비저장·링크 참조·공개 데이터 한정)과 충돌하지 않는다고 판단했다.
   - **개정 방식**: 이 저장소의 팩트가 `status: superseded` + `superseded_by`로 이력을 남기듯, 문서 조항도 삭제하지 않고 개정 표시를 남긴다. 구현(README 저작권 절, `LOREGUARD_DIRECTIVE §0.4`)과 문서가 어긋난 상태를 해소하기 위한 개정이다.
   - **개정일**: 2026-08-06 · **동기화 대상**: `README.md` 저작권·데이터 윤리 절, `LOREGUARD_DIRECTIVE.md §0.4`

---

## 7. 디자인 요구사항

- **방향**: 중세 다크 판타지 × 계측 도구. "고문서 보관소를 계기판으로 만든" 인상 — 세계관의 무게감과 QA 도구의 정밀함을 동시에.
- **금지**: 기본 템플릿 룩, 보라색 그라디언트 SaaS 클리셰, 게임 원본 아트를 UI 비주얼 아이덴티티(배경·장식·컴포넌트)로 사용하는 것. (연대기 화면의 공식 프로모션 스크린샷은 §6.6′ 개정에 따른 예외 — 삽화이지 디자인 언어가 아니다.)
- **시그니처 요소(1개)**: 검증 게이트의 판정 연출 — 판정 결과가 "인장(seal)이 찍히듯" 나타나는 모먼트. 나머지 화면은 절제.
- 세부 토큰(색·타이포·모션)은 지침서에서 확정한다.
- 품질 기준: 반응형(모바일까지), 키보드 포커스 가시성, reduced-motion 대응.

---

## 8. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 공개 세계관 텍스트 절대량 부족 | KB가 빈약해 검사가 공허해짐 | **조사로 해소됨** — 위키 3종 + 공식 소스로 성립 확인. 부족 영역은 confidence로 정직하게 표기 |
| LLM 오판(거짓 충돌·놓침) | 도구 신뢰도 하락 | 모든 판정에 근거 팩트 인용 강제. 근거 없는 판정은 WARN 이하로 강등. "판정 근거 보기"를 UI 1급 요소로 |
| 게임 출시 전 설정 대규모 개편 | KB 무효화 우려 | 오히려 기회 — F6이 개편을 흡수하는 과정 자체가 시연. superseded 이력이 포트폴리오 스토리가 됨 |
| API 비용 | 검사 전량 LLM 호출 시 비용 증가 | 규칙 기반 선별 후 LLM 호출(C3·C5), 검사 결과 캐시, 데모 시나리오는 사전 계산 결과 우선 표시 |
| 저작권 시비 | 프로젝트 신뢰 훼손 | §6 원칙의 코드 강제 + 전역 고지. 요청 시 즉시 비공개 전환 방침 명시 |

---

## 9. 마일스톤

| Phase | 산출물 | 완료 기준 |
|---|---|---|
| P0 프로젝트 셋업 | 리포지토리, 스키마 확정, 디자인 토큰 | 스키마 JSON 샘플 검증 통과 |
| P1 시드 지식베이스 | 엔티티 40+/팩트 120+/관계 60+ | §4 F1 시드 목록 전체 등록, 실증 충돌 3건 `conflicted` 상태 포함 |
| P2 검사 엔진 | C1~C5 + 리포트 생성 | 실증 사례 3건 자동 탐지 (G2) |
| P3 용어집 | 자동 편찬 + 내보내기 | CSV 열면 로컬라이제이션 담당이 바로 쓸 수 있는 형태 (G3) |
| P4 검증 게이트 | 내러티브 CI + 데모 시나리오 3종 | 30초 내 판정 + 근거 인용 (G4) |
| P5 복선 원장 + 변경 감지 | F5·F6 파이프라인 | 신규 소스 1건 유입 → 변경 리포트 + 재부상 탐지 시연 (G5·G6) |
| P6 대시보드 완성·배포 | 6개 화면, Vercel 배포, Playwright 검증 | 전 화면 스크린샷 검증 통과, 라이브 URL |

---

## 10. 오픈 퀘스천 (확인 필요)

1. 관계망 그래프의 우선순위 — 그래프 뷰를 1급으로 둘지, 리스트 중심 + 그래프 보조로 둘지 (개발 난도 차이 있음. 지침서는 리스트 우선 + 그래프 점진 강화로 작성해 둠).
2. 데모 대상 언어 — UI는 한국어 기본, 용어집·판정문은 한/영 병기. 영문 UI 토글은 v1 범위 외로 제안.
3. 공개 리포지토리 여부 — 채용용이면 공개가 유리하나, §6 고지 문구를 리포지토리 README에도 동일 적용 필요.
